<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Formulário</title>
</head>
<body>
  <h1>Formulário de Item</h1>
<form action="/itens/<%= item ? 'editar/' + item._id : 'novo' %>" method="POST">
<label for="nome">Nome:</label>
<input type="text" id="nome" name="nome" value="<%= item ? item.nome : '' %>">
<br>
<label for="descricao">Descrição:</label>
<textarea id="descricao" name="descricao"><%= item ? item.descricao : '' %></textarea>
<br>
<button type="submit">Salvar</button>
</form>
<a href="/itens">Voltar para a Lista</a>
</body>
</html>